import SessionLoadingOverlay from "@/components/SessionLoadingOverlay";

export default function Loading() {
  return <SessionLoadingOverlay title="Loading OB Bank" message="Getting the vault ready." />;
}
