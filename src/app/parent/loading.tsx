import SessionLoadingOverlay from "@/components/SessionLoadingOverlay";

export default function Loading() {
  return <SessionLoadingOverlay title="Opening parent portal" message="Loading family controls." />;
}
